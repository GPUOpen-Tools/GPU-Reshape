#include <Test/Device/ShaderHost.h>

// Shader
#include "$PATHD3D12.h"
#include "$PATHVulkan.h"

namespace {
    struct $NAMEInitializer {
        $NAMEInitializer() {
            ShaderHost::Register("$NAME", "D3D12", ShaderBlob{.code = kShaderCode$NAMED3D12, .length = sizeof(kShaderCode$NAMED3D12)});
            ShaderHost::Register("$NAME", "Vulkan", ShaderBlob{.code = kShaderCode$NAMEVulkan, .length = sizeof(kShaderCode$NAMEVulkan)});
        }
    } $NAMEInitializer;
}
